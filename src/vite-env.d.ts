/// <reference types="vite/client" />

/** Short commit SHA of the build, injected by vite.config.ts. */
declare const __COMMIT_SHA__: string;

/** "v{major}.{minor}.{patch}" build version, injected by vite.config.ts. */
declare const __APP_VERSION__: string;
